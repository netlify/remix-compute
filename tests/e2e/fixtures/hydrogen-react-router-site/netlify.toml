[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "24"
  # react-router's `module-sync` export makes Node resolve it to ESM under require(), which our
  # vite plugins do mid-load — tripping a require(esm) cycle bug (ERR_REQUIRE_CYCLE_MODULE) on
  # Node before 22.22/24. Opt out of require(esm) so react-router resolves to CJS instead. See
  # https://github.com/nodejs/node/issues/57869
  NODE_OPTIONS = "--no-experimental-require-module"

[dev]
  command = "npm run dev"

# Set immutable caching for static files, because they have fingerprinted filenames
[[headers]]
for = "/assets/*"
[headers.values]
"Cache-Control" = "public, max-age=31560000, immutable"

# These are only used to set up the template, and are not used by your project.
# You can safely delete this section.
# If you want to update the real values, change them in the Netlify app or via the Netlify CLI.
[template.environment]
PUBLIC_STORE_DOMAIN = "Store domain - set this to 'mock.shop' to try the demo site"
SESSION_SECRET = "Session secret - set this to a random value for production"
