import type { Config } from '@react-router/dev/config'

export default {
  // Config options...
  // Server-side render by default, to enable SPA mode set this to `false`
  ssr: true,
  // See https://reactrouter.com/api/framework-conventions/react-router.config.ts#basename
  basename: '/fr',
} satisfies Config
